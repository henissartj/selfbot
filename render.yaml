services:
  - type: web
    name: discord-selfbot-v4
    env: python
    buildCommand: pip uninstall -y discord.py; pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
